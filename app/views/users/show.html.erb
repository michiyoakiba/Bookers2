<p id="notice"><%= notice %></p>

<div class="container px-5 px-sm-0">
  <div class="row">
<div class="col-md-3">
  <h2>User info</h2>

<div>
  <%= image_tag @user.get_profile_image(100,100) %>
  <table class ="table">
    <tbody>
      <tr>
      <th>name</th>
      <th><%= @user.name %></th>
      </tr>
      <tr>
      <th>introduction</th>
      <th></th>
      </tr>
    </tbody>
  </table>
  <div class="row">
  </div>
  </div>

  <%= link_to edit_user_path(current_user.id), class: "btn btn-outline-secondary btn-block edit_user_#{@user.id}" do %>
  <i class="fas fa-user-cog"></i>
  <% end %>

 <h2 class = "mt-3">New book</h2>

<%= form_with model: @user, url: '/books', method: :post do |f| %>
  <h5>Tiile</h5>
  <%= f. text_field :title %>

  <h5>Opinion</h5>
  <%= f. text_area :body %>
</br>
  <%= f. submit 'Create Book',class:"btn btn-sm btn-success btn-block-auto" %>
<% end %>



</div>
 <div class="col-lg-8 offset-md-1">
   <h2>Books</h2>
<table class="table table-hover table-inverse">
  <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Opinion</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  </table>
</div>
</div>
</div>
